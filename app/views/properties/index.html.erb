<h1>Property index page</h1>

<%= link_to 'Map view', properties_map_path %>

<% @properties.each do |property| %>
  <div class="mt-4">
    <%= render "card", property: property %>
  </div>
<% end %>
